"""Как работают контекстные менеджеры?
Контекстный менеджер должен реализовать два специальных метода:

__enter__() → Выполняется при входе в блок with.
__exit__() → Выполняется при выходе из блока with."""

class FileManager:
    def __init__(self, filename, mode):
        self.filename = filename
        self.mode = mode
        self.file = None

    def __enter__(self):
        self.file = open(self.filename, self.mode)
        return self.file  # Возвращаем объект файла для использования в блоке with

    def __exit__(self, exc_type, exc_val, exc_tb):
        self.file.close()  # Закрываем файл
        return False  # Не подавляем исключения

# Использование
with FileManager("example.txt", "w") as file:
    file.write("This is a custom context manager.")

"""Преимущества использования контекстных менеджеров
✅ Автоматическое управление ресурсами: Гарантирует закрытие файлов, соединений и других ресурсов.
✅ Предотвращение утечек ресурсов: Избегает проблем с открытыми соединениями или использованием памяти.
✅ Чистый и читаемый код: Убирает необходимость явного вызова .close() или других методов очистки.

Контекстные менеджеры широко применяются для работы с файлами, подключениями к базам данных, блокировками потоков и сетевыми соединениями. 🚀"""
